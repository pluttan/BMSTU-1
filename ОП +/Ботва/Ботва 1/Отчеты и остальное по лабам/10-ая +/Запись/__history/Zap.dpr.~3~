program Zap;

uses
  Forms,
  chooseinit in 'chooseinit.pas' {Form1},
  inputUnit in 'inputUnit.pas' {Form2},
  lookingunit in 'lookingunit.pas' {Form3};

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TForm1, Form1);
  Application.CreateForm(TForm2, Form2);
  Application.CreateForm(TForm3, Form3);
  Application.Run;
end.
